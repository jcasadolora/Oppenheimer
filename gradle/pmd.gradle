pmd {
    toolVersion = pmdVersion
    ignoreFailures = false
}

tasks.withType(Pmd) {
    reports {
        xml.required = true
        html.required = true
    }
    // Include Groovy test files
    source = fileTree(dir: 'src/test/groovy', includes: ['**/*.groovy'])
}